# Mac bridge

```bash
pip3 install -r requirements.txt
python3 resolve_bridge.py -p /dev/cu.usbserial-XXXX
```

Shortcut-only bridge (no live Resolve API status).  
Full notes: [docs/bridge-and-status.md](../docs/bridge-and-status.md).
